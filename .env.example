# Neon Postgres connection strings
# DATABASE_URL - Pooled connection (for app runtime, has -pooler. in hostname)
DATABASE_URL=postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/database?sslmode=require
# DATABASE_URL_DIRECT - Direct connection (for migrations, no -pooler. in hostname)
DATABASE_URL_DIRECT=postgresql://user:password@ep-xxx.region.aws.neon.tech/database?sslmode=require

# Neon Auth (Better Auth)
# Get this from your Neon project's Auth settings (Auth → Configuration → Auth Base URL)
NEON_AUTH_BASE_URL=https://ep-xxx.neonauth.c-2.us-east-2.aws.neon.build/dbname/auth
NEXT_PUBLIC_NEON_AUTH_URL=https://ep-xxx.neonauth.c-2.us-east-2.aws.neon.build/dbname/auth

# The Odds API (https://the-odds-api.com/)
# Used for fetching betting odds data for watchability predictions
ODDS_API_KEY=your_api_key_here
